<!DOCTYPE html>
<html lang="en">

    <head>
        <%- include('includes/head.ejs') -%>
    </head>

    <body>

        <%- include('includes/header.ejs') -%>

        <div class="inner">

            <h1><%= title %></h1>

            <form action="/festivals/edituser" method="post">
                <div>
                    <label for="name">Naam:</label><br>
                    <input id="name" type="text" name="name" required>
                </div>
                
                <div>
                    <label for="year">Geboortejaar:</label><br>
                    <input id="year" name="year" type="number" min="1950" max="2005" step="1" value="2005" />
                </div>

                <div>
                    <fieldset>
                        <legend>Favoriete Genres:</legend>
                        <% genres.forEach(function(genre){ %>
                        <input type="checkbox" name="genres" value="<%= genre %>" id="<%= genre %>">
                        <label for="<%= genre %>"><%= genre %></label><br>
                        <% }) %>
                    </fieldset>
                </div>

                <button type="submit">Add</button>
            </form>
        </div>

        <%- include('includes/footer.ejs') -%>

    </body>

</html>